import { ReactNode } from "react";
interface PlanetCardProps {
    icon: ReactNode;
    name: string;
    description: string;
    planetColor: string;
    delay?: string;
}
declare const PlanetCard: ({ icon, name, description, planetColor, delay }: PlanetCardProps) => any;
export default PlanetCard;
//# sourceMappingURL=PlanetCard.d.ts.map